[project]
name = "healthcare-agent"
version = "0.1.0"
description = "Healthcare Agent Swarm Backend"
requires-python = ">=3.12.16"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["."]
